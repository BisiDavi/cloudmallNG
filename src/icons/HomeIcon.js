import React from "react";
import Image from "next/image";

const HomeIcon = () => {
    return (
        <div>
            <Image src="/static/images/home.svg" height={20} width={20} />
        </div>
    );
};

export default HomeIcon;
